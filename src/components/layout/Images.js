const images = [{ id: 'VER', src: './assets/VER.jpg' }];

export default images;

// const driverImageSrc = () => {
//   console.log(images);
//   let src;
//   for (let i = 0; i < images.length; i++) {
//     if (images[i].id === driverStandings.driver.code) {
//       src = images[i].src;
//       console.log(src);
//       return src;
//     }
//   }
// };

// useEffect(() => {
//   driverImageSrc();
//   // eslint-disable-next-line react-hooks/exhaustive-deps
// }, []);
